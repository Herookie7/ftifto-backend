name: Security Scan

on:
  push:
    branches:
      - main
    paths:
      - "**"
  pull_request:
    paths:
      - "**"
  schedule:
    - cron: "0 4 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    env:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
    defaults:
      run:
        working-directory: ftifto-backend
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: ftifto-backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run npm audit
        run: npm run audit

      - name: Run Snyk (optional)
        if: env.SNYK_TOKEN != ''
        uses: snyk/actions/node@master
        with:
          command: test
          args: --severity-threshold=high --project-name=ftifto-backend
        env:
          SNYK_TOKEN: ${{ env.SNYK_TOKEN }}

      - name: Snyk token not configured
        if: env.SNYK_TOKEN == ''
        run: echo "::notice::SNYK_TOKEN secret not configured; skipping Snyk scan."

